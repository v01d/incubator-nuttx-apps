if (CONFIG_TESTING_GETPRIME)
  nuttx_add_application(
    NAME ${CONFIG_TESTING_GETPRIME_PROGNAME}
    PRIORITY ${CONFIG_TESTING_GETPRIME_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_GETPRIME_STACKSIZE}
    SRCS getprime_main.c)
endif()
