if (CONFIG_TESTING_SMP)
  nuttx_add_application(
    NAME ${CONFIG_TESTING_SMP_PROGNAME}
    PRIORITY ${CONFIG_TESTING_SMP_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_SMP_STACKSIZE}
    SRCS smp_main.c)
endif()
