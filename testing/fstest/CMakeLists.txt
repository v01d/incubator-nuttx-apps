if (CONFIG_TESTING_FSTEST)
  nuttx_add_application(
    NAME ${CONFIG_TESTING_FSTEST_PROGNAME}
    PRIORITY ${CONFIG_TESTING_FSTEST_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_FSTEST_STACKSIZE}
    SRCS fstest_main.c)
endif()
