if (CONFIG_TESTING_SCANFTEST)
  nuttx_add_appplication(
    NAME ${CONFIG_TESTING_SCANFTEST_PROGNAME}
    PRIORITY ${CONFIG_TESTING_SCANFTEST_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_SCANFTEST_STACKSIZE}
    SRCS scanftest_main.c)
endif()
