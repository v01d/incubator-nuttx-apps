if (CONFIG_TESTING_SENSORTEST)
  nuttx_add_application(
    NAME ${CONFIG_TESTING_SENSORTEST_PROGNAME}
    PRIORITY ${CONFIG_TESTING_SENSORTEST_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_SENSORTEST_STACKSIZE}
    SRCS sensortest.c)
endif()
