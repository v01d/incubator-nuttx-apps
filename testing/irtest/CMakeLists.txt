if (CONFIG_TESTING_IRTEST)
  nuttx_add_application(
    NAME ${CONFIG_TESTING_IRTEST_PROGNAME}
    PRIORITY ${CONFIG_TESTING_IRTEST_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_IRTEST_STACKSIZE}
    SRCS main.cxx cmd.cxx enum.cxx)
endif()
