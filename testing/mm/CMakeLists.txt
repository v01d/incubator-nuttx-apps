if (CONFIG_TESTING_MM)
  nuttx_add_application(
    NAME ${CONFIG_TESTING_MM_PROGNAME}
    PRIORITY ${CONFIG_TESTING_MM_PRIORITY}
    STACKSIZE ${CONFIG_TESTING_MM_STACKSIZE}
    SRCS mm_main.c)
endif()
