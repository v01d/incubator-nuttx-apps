if (CONFIG_INTERPRETERS_MINIBASIC)
  nuttx_add_application(
    NAME basic
    PRIORITY ${CONFIG_INTERPRETER_MINIBASIC_PRIORITY}
    STACKSIZE ${CONFIG_INTERPRETER_MINIBASIC_STACKSIZE}
    SRCS script.c basic.c)
endif()
