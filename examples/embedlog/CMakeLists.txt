if (CONFIG_EXAMPLES_EMBEDLOG)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_EMBEDLOG_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_EMBEDLOG_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_EMBEDLOG_STACKSIZE}
    MAINSRC embedlog_main.c
  )
endif()
