if (CONFIG_EXAMPLES_WATCHED)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_WATCHED_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_WATCHED_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_WATCHED_STACKSIZE}
    SRCS watched_main.c watched.c
  )
endif()
