if (CONFIG_EXAMPLES_UID)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_UID_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_UID_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_UID_STACKSIZE}
    SRCS uid_main.c
  )
endif()
