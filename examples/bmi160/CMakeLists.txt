if(CONFIG_EXAMPLES_SIXAXIS)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_SIXAXIS_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_SIXAXIS_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_SIXAXIS_STACKSIZE}
    SRCS sixaxis_main.c
  )
endif()
