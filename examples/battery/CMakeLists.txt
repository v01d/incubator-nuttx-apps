if(CONFIG_EXAMPLES_BATTERY)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_BATTERY_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_BATTERY_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_BATTERY_STACKSIZE}
    SRCS batt_main.c
  )
endif()
