if(CONFIG_EXAMPLES_CHRONO)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_CHRONO_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_CHRONO_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_CHRONO_STACKSIZE}
    SRCS chrono_main.c
  )
endif()
