if (CONFIG_EXAMPLES_IPERF)
  nuttx_add_application(
    NAME ${CONFIG_EXAMPLES_IPERF_PROGNAME}
    PRIORITY ${CONFIG_EXAMPLES_IPERF_PRIORITY}
    STACKSIZE ${CONFIG_EXAMPLES_IPERF_STACKSIZE}
    SRCS iperf_main.c iperf.c)
endif()
