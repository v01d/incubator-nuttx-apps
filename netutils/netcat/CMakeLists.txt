if (CONFIG_NETUTILS_NETCAT)
  nuttx_add_application(
    PRIORITY ${CONFIG_NETUTILS_NETCAT_PRIORITY}
    NAME ${CONFIG_NETUTILS_NETCAT_PROGNAME}
    STACKSIZE ${CONFIG_NETUTILS_NETCAT_STACKSIZE}
    SRCS netcat_main.c)
endif()
