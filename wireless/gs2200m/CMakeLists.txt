if (CONFIG_WIRELESS_GS2200M)
  nuttx_add_application(
    NAME ${CONFIG_WIRELESS_GS2200M_PROGNAME}
    PRIORITY ${CONFIG_WIRELESS_GS2200M_PRIORITY}
    STACKSIZE ${CONFIG_WIRELESS_GS2200M_STACKSIZE}
    SRCS gs2200m_main.c)
endif()
