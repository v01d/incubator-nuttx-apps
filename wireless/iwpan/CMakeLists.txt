if (CONFIG_WIRELESS_IWPAN)
  nuttx_add_application(
    NAME ${CONFIG_WIRELESS_IWPAN_PROGNAME}
    PRIORITY ${CONFIG_WIRELESS_IWPAN_PRIORITY}
    STACKSIZE ${CONFIG_WIRELESS_IWPAN_STACKSIZE}
    SRCS src/iwpan.c)
endif()
